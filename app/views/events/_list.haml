%ul
  - for event in @events
    %li
      = event.owner == @current_user ? "You" : event.owner.name
      = event.category
      - if event.target && event.target.title
        = event.target.title
      == (#{time_ago_in_words(event.created_at)} ago)
