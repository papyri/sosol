#main
  = render :partial => 'edit_bar'
  .site
    %h1 Editing document

    - form_for(@document) do |f|
      = f.error_messages

      %p
        = f.label :leiden
        %br
        = f.text_area :leiden, :cols => 80, :rows => 40, :wrap => 'off', :class => 'greek'
    
      = render :partial => 'edit_commit', :locals => { :f => f }
