<h1>New E-mailer for <%= @board.title %></h1>

<% form_for(@emailer) do |f| %>
  <%= f.error_messages %>
    <%= f.hidden_field :board_id %>  
  <!--
  <p>
    <%= f.label :when_to_send %><br />
    <%= f.text_field :when_to_send %>
  </p>
  <p>
    <%= f.label :include_document %>
    <%= f.check_box :include_document %>
  </p>
  <p>
    <%= f.label :message %><br />
    <%= f.text_area :message %>
  </p>
  -->
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', emailers_path %>
