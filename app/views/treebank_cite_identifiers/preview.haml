= javascript_include_tag 'confirm'
= javascript_include_tag 'treebank-cite'
  
#main
  = render :partial => 'identifiers/edit_bar'

  .site
    #edit
      = form_for @identifier, :url => { :action => :update }  do |f|
        = f.error_messages
        %h2 Preview
        .hints Click a sentence to view it in Alpheios
        .PContent
          = @identifier[:html_preview].html_safe
       
