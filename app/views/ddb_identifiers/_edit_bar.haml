#article_menu
  .site
    %ul
      %li#active= link_to 'Edit', edit_polymorphic_path([@publication, @identifier])
      %li= link_to 'History'

#article_sub_menu
  .site
    .joiner
    %ul
      %li{ :class => params[:action] == 'edit' ? 'active' : ''}= link_to 'Leiden+', edit_polymorphic_path([@publication, @identifier])
      %li{ :class => params[:action] == 'editxml' ? 'active' : ''}= link_to 'XML'#, editxml_publication_path(@publication)
      %li{ :class => params[:action] == 'preview' ? 'active' : ''}= link_to 'Preview'#, preview_publication_path(@publication)
      %li{ :class => params[:action] == 'comment_on' ? 'active' : ' '}= link_to 'Comment'#, comment_on_publication_path(@publication)
