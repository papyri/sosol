- form_for :publication, Publication.new, :url => { :controller => "publications", :action =>"create_from_list" } do |form|

  = form.submit "Create", :class => ''
  = label_tag 'pn_id_list', 'Create publication from PN IDs, one per line:'
  = text_area_tag 'pn_id_list', '',  :cols => '40', :rows => '100'

