= stylesheet_link_tag "board"

%h1
  Community Members
Click user to add to community.
- if @community
  
  = render :partial => "user/select_user_by_name", :locals => { :li => LinkingInfo.new("communities", "add_member", @community.id ) }

- else
  = "nil com"