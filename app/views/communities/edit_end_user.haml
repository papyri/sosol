

= stylesheet_link_tag "board"

%h1
  Community End User
Click user to set as end repository for community publications.
- if @community
  
  = render :partial => "user/select_user_by_name", :locals => { :li => LinkingInfo.new("communities", "set_end_user", @community.id ) }

- else
  = "nil com"